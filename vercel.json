{
  "buildCommand": "pnpm install && pnpm --filter libremercado build",
  "outputDirectory": "artifacts/libremercado/dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
