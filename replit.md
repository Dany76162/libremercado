# Despliegue

Este repositorio **no** está pensado para producción en Replit.

Arquitectura objetivo: **Vercel** (frontend `artifacts/libremercado`), **Railway** (API `artifacts/api-server` + PostgreSQL), **Cloudflare R2** (objetos vía S3). Variables de entorno: ver `.env.example` en la raíz.
