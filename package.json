{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"process.exit(0)\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:docker": "docker compose up -d",
    "dev:wait-db": "pnpm exec wait-on tcp:127.0.0.1:5434 -t 120000",
    "db:push": "node scripts/run-db-push.mjs",
    "dev:api": "node scripts/run-api-dev.mjs",
    "dev:web": "node scripts/run-web-dev.mjs",
    "dev": "pnpm run dev:docker && pnpm run dev:wait-db && pnpm run db:push && concurrently -k -n api,web -c cyan,magenta \"pnpm run dev:api\" \"pnpm run dev:web\""
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2",
    "wait-on": "^9.0.5"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "0.27.4",
      "@esbuild/win32-x64": "0.27.4"
    }
  }
}