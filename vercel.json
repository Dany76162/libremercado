{
  "buildCommand": "pnpm install && pnpm --filter libremercado build",
  "outputDirectory": "artifacts/libremercado/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
